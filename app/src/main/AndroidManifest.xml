<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.neto.activitys">

    <!-- To auto-complete the email text field in the com.example.neto.projetofinal.activitys.login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.neto.projetofinal.activitys.login.LoginActivity" />
        <activity android:name="com.example.neto.projetofinal.activitys.login.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.neto.projetofinal.activitys.MainActivity"
            android:label="@string/title_activity_main"
            android:parentActivityName="com.example.neto.projetofinal.activitys.login.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.neto.projetofinal.activitys.login.LoginActivity" />
        </activity>

        <service
            android:name="com.example.neto.projetofinal.services.AtualizarEventosService"
            android:exported="false"></service>
    </application>

</manifest>